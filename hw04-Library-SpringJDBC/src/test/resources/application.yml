spring:
  datasource:
    url: jdbc:h2:mem:Library;DB_CLOSE_DELAY=-1
    username: user
    password: password
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always